import React, { Component } from 'react';
import { ScrollView } from 'react-native';

class Me extends Component {
  render() {
    return (
      <ScrollView>

      </ScrollView>
    );
  }
}

export default Me;
